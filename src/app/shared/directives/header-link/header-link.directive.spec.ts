import { HeaderLinkDirective } from './header-link.directive';

describe('HeaderLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderLinkDirective();
    expect(directive).toBeTruthy();
  });
});
